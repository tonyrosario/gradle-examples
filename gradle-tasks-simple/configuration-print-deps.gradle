configurations {
    something
}

repositories {
    mavenCentral()
}

dependencies {
    something 'junit:junit:4.11'
}

task printDeps(description: "Print dependencies") << {
    configurations.something.each {
        println(it.name)
    }
}
